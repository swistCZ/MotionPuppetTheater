{
  "name": "web-motion-puppet-theater",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@mediapipe/camera_utils": "latest",
    "@mediapipe/drawing_utils": "latest",
    "@mediapipe/hands": "latest",
    "fflate": "^0.8.3",
    "gifenc": "^1.0.3",
    "pixi.js": "^8.8.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.1",
    "vitest": "^3.0.5"
  }
}
